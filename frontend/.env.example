# Copy to .env.local for local overrides (Vite loads .env.local automatically, gitignored by default).
# Not needed for local dev - vite.config.ts already proxies /api to localhost:8080.
# Required in production: point this at your deployed backend, including the /api context path.
VITE_API_URL=https://your-backend.onrender.com/api
